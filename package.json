{
  "name": "openorca",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "pnpm": {
    "onlyBuiltDependenciesFile": "./trusted-dependencies-scripts.json",
    "overrides": {
      "@visual-json/react>@visual-json/core": "0.1.0"
    }
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "portless": "^0.4.1",
    "turbo": "^2.4.0",
    "typescript": "^5.9.3"
  }
}
